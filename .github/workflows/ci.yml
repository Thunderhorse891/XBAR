name: ci

on:
  push:
    branches:
      - main
      - 'codex/**'
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Run tests
        run: npm test

      - name: Build app
        run: npm run build

      - name: Install Playwright browser
        # Prefer the full install (browser + system deps). GitHub's
        # ubuntu-latest image already ships Chromium's runtime libraries, so
        # fall back to a deps-free browser install when the runner's apt
        # sources are temporarily broken (e.g. an unsigned upstream repo).
        run: npx playwright install --with-deps chromium || npx playwright install chromium

      - name: Production smoke test (built dist)
        run: npm run test:prod-smoke

      # Guards the App Store rules that only apply to the native bundle: no
      # non-IAP purchase path (Guideline 3.1.1), no third-party sign-in that
      # cannot complete in a WebView, and no marketing links that dead-end
      # (2.1). These are build-flag differences, so the web suites cannot see
      # them — this builds the store bundle and asserts on what it renders.
      - name: Store build smoke (App Store compliance)
        run: npm run test:mobile-smoke

      - name: Upload smoke test traces on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: prod-smoke-test-results
          path: test-results/
          if-no-files-found: ignore
          retention-days: 7
