# Leave empty to run against the built-in mock data layer.
# Point at the XBAR backend (Vercel serverless API) to go live.
NEXT_PUBLIC_API_URL=

# Stripe publishable key for client-side checkout (optional in mock mode).
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
